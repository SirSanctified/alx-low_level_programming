#include "main.h"

/**
 * _abs - find absolute value of a number
 * @num: number to be checked
 *
 * Description: find absolute
 *
 * Return: absolute of num
 */

int _abs(int num)
{
	return (num < 0 ? -num : num);
}
